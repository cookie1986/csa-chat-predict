--- All the csv files in the Data folder will later be deleted, so make copies ---

## requirements pandas, plotly, os, glob ##

-Insert all the .csv that are to be analysed in the Data folder

-Run Code
	-Creates folders for all the .csv with graphs for all the local metrics.
	-Updates and saves the .csv file in its folder with metrics
	-Saves a csv that is indexed per minute the people have messaged each other with metrics
	-Creates graphs for per message csv

	-Creates a Mean Global Metrics csv in the Data folder
